	<div class="nested-fields">
	<table class="table table-striped table-bordered">
		<tr>
			<td>
				<%= f.label :durée%>
			</td>
			<td>
				<%= f.text_field :hours, class: 'form-control' %>Heures <%= f.text_field :minutes, class: 'form-control'%></td>
		</tr>
		<tr>
			<td>
				<%= f.label :description%>
			</td>
			<td>
				<%= f.text_area :description, class: "form-control"%>
				<%= f.hidden_field :worker_id, :value => current_user.id%>
				<%= f.hidden_field :project_id, :value => @project.id%>
			</td>
		</tr>
		</table>
		<p class="text-left">
		<%= link_to_remove_association "Retirer le travail", f, class: "btn btn-danger" %>
		</p>
		</div>
